find_package(Threads)

add_library(FileOperations STATIC fileOperations.cpp)

target_include_directories(FileOperations PUBLIC
    "${PROJECT_BINARY_DIR}"
    "${PROJECT_SOURCE_DIR}/src/include"
)

target_link_libraries(FileOperations
    "${CMAKE_THREAD_LIBS_INIT}"
)

install(TARGETS Graph LIBRARY DESTINATION ${PROJECT_SOURCE_DIR}/lib)